#include <Arduino.h>
#include <WaveGenerator.h>

WaveGenerator waveGenerator(
    {1000, 2000, 3000},  // Frequencies in Hz
    1000                 // Wave duration in ms
);
void setup() { }

void loop() {
  // put your main code here, to run repeatedly:
}
